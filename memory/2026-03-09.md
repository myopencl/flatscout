
## 20:01 CET - OLX Fallback Configuration & Cron Assessment
- **OLX API Fallback Implemented**: Replaced the failing OLX `district_id` search with two discrete city-wide searches for Poznań to bypass 400 Bad Request errors.
  - Search 1: 2 Rooms, 400k-600k PLN, min 40m² (ID: `d3214cb7-a79b-4176-83a1-65da66d1f7dd`)
  - Search 2: 3 Rooms, 400k-600k PLN, min 40m² (ID: `1ea4cd9c-6248-4318-bbd8-e44d8750c8ce`)
- **Model Reverted**: Reverted session model from `openai-codex/gpt-5.3-codex` override back to the system default (`openrouter/auto`) at user's request.
- **CRON Setup Verification**: Checked `crontab -l` for the requested 20:00 trigger. Found existing job `0 */6 * * *` (00:00, 06:00, 12:00, 18:00) which misses the 20:00 target. 
- **Script Analysis**: `update_and_deploy.sh` writes logs directly to `/var/log/flatscout-deploy.log` via standard user cron, which might fail due to lack of `sudo` write permissions depending on group setup. Pending permission check or path redirection before enabling the 20:00 schedule.

## 20:44 CET - OLX Searches Disabled
- **Disabled OLX Searches**: Both fallback OLX searches for Poznań (2 and 3 rooms) have been manually, explicitly disabled via PATCH requests to the API. They will no longer run automatically.
  - Disabled ID: `d3214cb7-a79b-4176-83a1-65da66d1f7dd`
  - Disabled ID: `1ea4cd9c-6248-4318-bbd8-e44d8750c8ce`
